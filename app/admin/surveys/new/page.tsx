export default function NewSurveyPage() {
  // TODO: form to configure topic, sponsor, question guide, tone,
  // num_questions, gift_card_amount, custom_fields, then insert into
  // Supabase `surveys`.
  return (
    <div>
      <h1 className="text-xl font-semibold">New Survey</h1>
    </div>
  );
}
